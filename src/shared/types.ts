export enum SelectedPage {
  Home = "home",
  AboutMe = "aboutme",
  Projects = "projects",
  Contact = "contact"
  }

export interface ImageType {
  date?: string;
  location: string;
  image: string;
}